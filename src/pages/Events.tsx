import Hero from "../components/Hero";
import heroImage from "../assets/events-hero.jpg";

function EventsPage() {
   return (
      <>
         <Hero title="Events" background={heroImage} />
      </>
   );
}

export default EventsPage;
